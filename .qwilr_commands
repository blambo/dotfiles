# Source commands that we don't want stored in git repo
if [ -s ~/.private-qwilr-commands ]
then
	source ~/.private-qwilr-commands
fi

# Update if development path changes
export qfolder=~/dev/qwilr

source $qfolder/BashScripts.sh

